{
  "id": "contract-003",
  "category": "contract",
  "prompt": "Implement `clampTo : Nat, Nat, Nat -> Nat` (value, lo, hi) satisfying: result is within [lo, hi] and equals value when value already in range. Assume lo <= hi.",
  "scope": [
    { "name": "Nat.max", "ty": "Nat, Nat -> Nat" },
    { "name": "Nat.min", "ty": "Nat, Nat -> Nat" }
  ],
  "grade": { "compile": true, "tests": ["tests/clamp_to_spec.claw"], "contracts": ["result >= lo", "result <= hi"], "forbidden": ["hallucinated-symbol"] },
  "reference": "solutions/contract-003.claw"
}
