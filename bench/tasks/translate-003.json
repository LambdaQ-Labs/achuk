{
  "id": "translate-003",
  "category": "translate",
  "prompt": "Translate this JavaScript to Achuk:\n\nfunction firstMatch(xs, p) {\n  for (const x of xs) if (p(x)) return x;\n  return null;\n}\n\nTarget signature: `firstMatch : List a, (a -> Bool) -> Maybe a`. Use only in-scope symbols.",
  "scope": [
    { "name": "List.find", "ty": "List a, (a -> Bool) -> Maybe a" },
    { "name": "List.filter", "ty": "List a, (a -> Bool) -> List a" },
    { "name": "List.head", "ty": "List a -> Maybe a" }
  ],
  "grade": {
    "compile": true,
    "tests": ["tests/first_match_spec.achuk"],
    "contracts": [],
    "forbidden": ["hallucinated-symbol"]
  },
  "reference": "solutions/translate-003.achuk"
}
