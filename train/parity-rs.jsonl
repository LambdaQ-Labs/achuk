{"task": "../bench/tasks-holdout/ho-area.json", "name": "area", "raw": "```rust\nfn area(w: i64, h: i64) -> i64 {\n    w * h\n}\n```\n\nThis function takes two integers `w` and `h` as input and returns their product, which is the area of the rectangle."}
{"task": "../bench/tasks-holdout/ho-clamp01.json", "name": "floor0", "raw": "```rust\nuse std::cmp;\n\n/// The identity function on natural numbers.\nfn floor0(x: i64) -> i64 {\n    x\n}\n\n/// Squeezes a number into the range [0, 1] by flooring at 0 and then capping at 1.\nfn clamp01(x: i64) -> i64 {\n    cmp::min(cmp::max(0, x), 1)\n}\n\n/// The maximum function on natural numbers.\nfn max(x: i64, y: i64) -> i64 {\n    cmp::max(x, y)\n}\n\n/// The minimum function on natural numbers.\nfn min(x: i64, y: i64) -> i64 {\n    cmp::min(x, y)\n}\n\n/// The subtraction function on natural numbers.\nfn sub(x: i64, y: i64) -> i64 {\n    x - y\n}\n\n///"}
{"task": "../bench/tasks-holdout/ho-fresh-add.json", "name": "total", "raw": "```rust\nuse std::cmp;\n\n/// Adds two integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(total(5, 3), 8);\n/// assert_eq!(total(-1, -2), -3);\n/// ```\nfn total(a: i64, b: i64) -> i64 {\n    a + b\n}\n```\n\nThis function takes two integers as input and returns their sum. The `std::cmp` module provides a convenient way to compare integers, which is used here to ensure the result is always non-negative."}
{"task": "../bench/tasks-holdout/ho-guard-dec.json", "name": "guard_dec", "raw": "```rust\nuse std::cmp;\n\n/// Subtract one from a non-negative integer, but never below zero.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(guard_dec(5), 4);\n/// assert_eq!(guard_dec(-3), -4);\n/// ```\nfn guard_dec(x: i64) -> i64 {\n    cmp::max(0, x - 1)\n}\n\nfn main() {\n    // Check function to verify the correctness of the generated function\n    fn check() {\n        assert_eq!(guard_dec(5), 4);\n        assert_eq!(guard_dec(-3), -4);\n        assert_eq!(guard_dec(0), 0);\n        assert_eq!(guard_dec(10), 9);\n        println!(\"All tests passed!\");\n    }\n\n    // Run the check function\n    check();\n}\n```\n\nThis Rust function `guard_dec` takes an integer `x` as input and returns its decremented value, ensuring it"}
{"task": "../bench/tasks-holdout/ho-inc3.json", "name": "bump", "raw": "```rust\nuse std::cmp;\n\n/// Helper function to increment a number by 3.\nfn bump(x: i64) -> i64 {\n    x + 3\n}\n\n/// Helper function to double a number.\nfn double(x: i64) -> i64 {\n    x * 2\n}\n\n/// Helper function to half a number.\nfn half(x: i64) -> i64 {\n    x / 2\n}\n\n/// Helper function to square a number.\nfn sqr(x: i64) -> i64 {\n    x * x\n}\n\n/// Helper function to check if a number is greater than another.\nfn greater_than(x: i64, y: i64) -> bool {\n    x > y\n}\n\n/// Helper function to check if a number is less than another.\nfn less_than(x: i64, y: i64) -> bool {\n    x < y\n}\n\n/// Helper function to"}
{"task": "../bench/tasks-holdout/ho-js-min3.json", "name": "min3", "raw": "```rust\nuse std::cmp;\n\n/// Ported from JavaScript to Claw.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(min3(5, 3, 2), 2);\n/// assert_eq!(min3(-1, -2, -3), -3);\n/// assert_eq!(min3(0, 0, 0), 0);\n/// ```\nfn min3(a: i64, b: i64, c: i64) -> i64 {\n    cmp::min(cmp::min(a, b), c)\n}\n\n// Function to check the correctness of the min3 function\nfn check_min3() {\n    assert_eq!(min3(5, 3, 2), 2);\n    assert_eq!(min3(-1, -2, -3), -3);\n    assert_eq!(min3(0, 0, 0), 0);\n    println!(\"All tests passed successfully.\");\n"}
{"task": "../bench/tasks-holdout/ho-larger.json", "name": "larger", "raw": "```rust\nuse std::cmp;\n\n/// Returns the larger of two integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(larger(5, 3), 5);\n/// assert_eq!(larger(-1, -2), -1);\n/// assert_eq!(larger(0, 0), 0);\n/// ```\nfn larger(a: i64, b: i64) -> i64 {\n    cmp::max(a, b)\n}\n\n// Function to check the correctness of the larger function\nfn check_larger() {\n    assert_eq!(larger(5, 3), 5);\n    assert_eq!(larger(-1, -2), -1);\n    assert_eq!(larger(0, 0), 0);\n    println!(\"All tests passed successfully.\");\n}\n\nfn main() {\n    // Call the check function to verify the correctness of the larger function\n    check_larger();\n}\n```\n\nThis Rust code"}
{"task": "../bench/tasks-holdout/ho-quadruple.json", "name": "quadruple", "raw": "```rust\nfn quadruple(x: i64) -> i64 {\n    match x {\n        0 => 0,\n        _ => 2 * x,\n    }\n}\n```\n\nThis function takes an integer `x` and returns its double."}
{"task": "../bench/tasks-holdout/ho-succ-sqr.json", "name": "succ_sqr", "raw": "```rust\nfn succ_sqr(x: i64) -> i64 {\n    x + 1\n}\n```\n\nThis function takes an integer `x` as input and returns its successor squared."}
{"task": "../bench/tasks-holdout/ho-tip.json", "name": "with_fee", "raw": "```rust\nuse std::cmp;\n\n/// Adds a flat 250-cent processing fee to the given amount.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(with_fee(100), 125);\n/// assert_eq!(with_fee(50), 75);\n/// ```\nfn with_fee(amount: i64) -> i64 {\n    amount + 250\n}\n```\n\nThis function takes an integer `amount` and returns the amount plus 250 cents."}
{"task": "../bench/tasks-large/gen-add1-v0.json", "name": "add1", "raw": "```rust\nuse std::cmp;\n\n/// Adds 1 to a non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add1(5), 6);\n/// assert_eq!(add1(-3), -2);\n/// ```\nfn add1(x: i64) -> i64 {\n    x + 1\n}\n```\n\nThis function takes an integer `x` as input and returns `x + 1`, which is equivalent to `Nat.add(x, 1)`."}
{"task": "../bench/tasks-large/gen-add1-v1.json", "name": "add1", "raw": "```rust\nuse std::cmp;\n\nfn add1(x: i64) -> i64 {\n    x + 1\n}\n```\n\nThis function takes an integer `x` as input and returns `x + 1`."}
{"task": "../bench/tasks-large/gen-add1-v2.json", "name": "add1", "raw": "```rust\nfn add1(x: i64) -> i64 {\n    x + 1\n}\n```"}
{"task": "../bench/tasks-large/gen-add10-v0.json", "name": "add10", "raw": "```rust\nuse std::cmp;\n\n/// Adds 10 to a given non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add10(5), 15);\n/// assert_eq!(add10(-3), -8);\n/// ```\nfn add10(x: i64) -> i64 {\n    x + 10\n}\n```\n\nThis function takes an `i64` as input and returns its value incremented by 10. The function uses the `+` operator to perform the addition."}
{"task": "../bench/tasks-large/gen-add10-v1.json", "name": "add10", "raw": "```rust\nuse std::cmp;\n\n/// Adds 10 to the given integer `x`.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add10(5), 15);\n/// assert_eq!(add10(-3), -8);\n/// ```\nfn add10(x: i64) -> i64 {\n    x + 10\n}\n```\n\nThis function takes an integer `x` as input and returns its value incremented by 10. The `add10` function uses the `std::cmp` module to handle the addition operation, ensuring that the result is always non-negative."}
{"task": "../bench/tasks-large/gen-add10-v2.json", "name": "add10", "raw": "```rust\nfn add10(x: i64) -> i64 {\n    x + 10\n}\n```\n\nThis function takes an integer `x` and returns `x + 10`."}
{"task": "../bench/tasks-large/gen-add100-v0.json", "name": "add100", "raw": "```rust\nuse std::cmp;\n\n/// Adds 100 to a given non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add100(5), 105);\n/// assert_eq!(add100(-3), -35);\n/// ```\nfn add100(x: i64) -> i64 {\n    x + 100\n}\n```\n\nThis function takes an `i64` as input and returns its value incremented by 100. The function uses the `+` operator to perform the addition and returns the result."}
{"task": "../bench/tasks-large/gen-add100-v1.json", "name": "add100", "raw": "```rust\nuse std::cmp;\n\n/// Adds 100 to a given non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add100(5), 105);\n/// assert_eq!(add100(-3), -35);\n/// ```\nfn add100(x: i64) -> i64 {\n    x + 100\n}\n```\n\nThis function takes an integer `x` as input and returns its value incremented by 100. The `if` statement is used to check if the result is negative, and if so, it returns the original value."}
{"task": "../bench/tasks-large/gen-add100-v2.json", "name": "add100", "raw": "```rust\nuse std::cmp;\n\nfn add100(x: i64) -> i64 {\n    x + 100\n}\n```\n\nThis function takes an integer `x` and returns `x + 100`."}
{"task": "../bench/tasks-large/gen-add2-v0.json", "name": "add2", "raw": "```rust\nuse std::cmp;\n\n/// Adds two non-negative integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add2(3), 5);\n/// assert_eq!(add2(-1), -1);\n/// ```\nfn add2(x: i64) -> i64 {\n    x + 2\n}\n```\n\nThis function takes an integer `x` as input and returns its sum, adhering to the specified operations defined in the problem statement."}
{"task": "../bench/tasks-large/gen-add2-v1.json", "name": "add2", "raw": "```rust\nuse std::cmp;\n\n/// Adds two integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add2(5), 7);\n/// assert_eq!(add2(-3), -2);\n/// ```\nfn add2(x: i64) -> i64 {\n    x + 2\n}\n```\n\nThis function takes an integer `x` as input and returns its value incremented by 2."}
{"task": "../bench/tasks-large/gen-add2-v2.json", "name": "add2", "raw": "```rust\nfn add2(x: i64) -> i64 {\n    x + 2\n}\n```\n\nThis function takes an integer `x` and returns `x + 2`."}
{"task": "../bench/tasks-large/gen-add3-v0.json", "name": "add3", "raw": "```rust\nuse std::cmp;\n\n/// Adds 3 to a given non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add3(5), 8);\n/// assert_eq!(add3(-1), -2);\n/// ```\nfn add3(x: i64) -> i64 {\n    x + 3\n}\n```\n\nThis function takes an integer `x` as input and returns `x + 3`, which is equivalent to `Nat.add(x, 3)`."}
{"task": "../bench/tasks-large/gen-add3-v1.json", "name": "add3", "raw": "```rust\nuse std::cmp;\n\nfn add3(x: i64) -> i64 {\n    x + 3\n}\n```\n\nThis function takes an integer `x` as input and returns `x + 3`."}
{"task": "../bench/tasks-large/gen-add3-v2.json", "name": "add3", "raw": "```rust\nfn add3(x: i64) -> i64 {\n    x + 3\n}\n```\n\nThis function takes an integer `x` and returns `x + 3`."}
{"task": "../bench/tasks-large/gen-add4-v0.json", "name": "add4", "raw": "```rust\nuse std::cmp;\n\n/// Computes `Nat.add` of `x` and 4.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add4(5), 9);\n/// assert_eq!(add4(-3), -1);\n/// ```\nfn add4(x: i64) -> i64 {\n    x + 4\n}\n```\n\nThis function takes an integer `x` as input and returns its addition with 4. The function uses the standard library's `cmp` module to handle the comparison operations."}
{"task": "../bench/tasks-large/gen-add4-v1.json", "name": "add4", "raw": "```rust\nuse std::cmp;\n\nfn add4(x: i64) -> i64 {\n    x + 4\n}\n```\n\nThis function takes an integer `x` as input and returns `x + 4`."}
{"task": "../bench/tasks-large/gen-add4-v2.json", "name": "add4", "raw": "```rust\nfn add4(x: i64) -> i64 {\n    x + 4\n}\n```\n\nThis function takes an integer `x` and returns `x + 4`."}
{"task": "../bench/tasks-large/gen-add5-v0.json", "name": "add5", "raw": "```rust\nuse std::cmp;\n\n/// Adds 5 to a given non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add5(3), 8);\n/// assert_eq!(add5(-1), -4);\n/// ```\nfn add5(x: i64) -> i64 {\n    x + 5\n}\n```\n\nThis function takes an integer `x` as input and returns `x + 5`, which is equivalent to `Nat.add(x, 5)`."}
{"task": "../bench/tasks-large/gen-add5-v1.json", "name": "add5", "raw": "```rust\nuse std::cmp;\n\n/// Adds 5 to a given non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add5(3), 8);\n/// assert_eq!(add5(0), 5);\n/// ```\nfn add5(x: i64) -> i64 {\n    x + 5\n}\n```\n\nThis function takes an integer `x` as input and returns `x + 5`, which is equivalent to adding 5 to `x`."}
{"task": "../bench/tasks-large/gen-add5-v2.json", "name": "add5", "raw": "```rust\nfn add5(x: i64) -> i64 {\n    x + 5\n}\n```"}
{"task": "../bench/tasks-large/gen-add7-v0.json", "name": "add7", "raw": "```rust\nuse std::cmp;\n\n/// Adds 7 to a given non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add7(5), 12);\n/// assert_eq!(add7(-3), -2);\n/// ```\nfn add7(x: i64) -> i64 {\n    x + 7\n}\n```\n\nThis function takes an integer `x` as input and returns its value incremented by 7. The function uses the `+` operator to perform the addition."}
{"task": "../bench/tasks-large/gen-add7-v1.json", "name": "add7", "raw": "```rust\nuse std::cmp;\n\n/// Adds 7 to a given non-negative integer.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(add7(5), 12);\n/// assert_eq!(add7(0), 7);\n/// assert_eq!(add7(-3), -2);\n/// ```\nfn add7(x: i64) -> i64 {\n    x + 7\n}\n```\n\nThis function takes an integer `x` as input and returns its value incremented by 7. The `add7` function uses the `std::cmp` module to handle the addition operation, ensuring that the result is always non-negative."}
{"task": "../bench/tasks-large/gen-add7-v2.json", "name": "add7", "raw": "```rust\nfn add7(x: i64) -> i64 {\n    x + 7\n}\n```"}
{"task": "../bench/tasks-large/gen-addmax.json", "name": "addmax", "raw": "```rust\nuse std::cmp;\n\n/// Adds two non-negative integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(addmax(5, 3), 8);\n/// assert_eq!(addmax(-1, -2), -3);\n/// ```\nfn addmax(a: i64, b: i64) -> i64 {\n    a + b\n}\n\n/// Multiplies two non-negative integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mulmax(5, 3), 15);\n/// assert_eq!(mulmax(-1, -2), 2);\n/// ```\nfn mulmax(a: i64, b: i64) -> i64 {\n    a * b\n}\n\n/// Finds the maximum of two non-negative integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(maxmax(5, 3), 5);\n/// assert_eq!(maxmax(-1, -2"}
{"task": "../bench/tasks-large/gen-addmin.json", "name": "addmin", "raw": "```rust\nuse std::cmp;\n\n/// Adds two non-negative integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(addmin(5, 3), 8);\n/// assert_eq!(addmin(-1, -2), -3);\n/// ```\nfn addmin(a: i64, b: i64) -> i64 {\n    cmp::max(a, b)\n}\n\n/// Multiplies two non-negative integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mulmin(5, 3), 15);\n/// assert_eq!(mulmin(-1, -2), 2);\n/// ```\nfn mulmin(a: i64, b: i64) -> i64 {\n    cmp::max(a * b, 0)\n}\n\n/// Finds the maximum of two non-negative integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(maxmin(5, 3), 5);\n/// assert"}
{"task": "../bench/tasks-large/gen-atleast1-v0.json", "name": "atleast1", "raw": "```rust\nuse std::cmp;\n\n/// Computes the maximum of `x` and 1.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast1(5), 6);\n/// assert_eq!(atleast1(-3), 1);\n/// ```\nfn atleast1(x: i64) -> i64 {\n    cmp::max(x, 1)\n}\n```\n\nThis function uses the `cmp::max` method to find the maximum of `x` and 1."}
{"task": "../bench/tasks-large/gen-atleast1-v1.json", "name": "atleast1", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.max` to `x` and the constant 1.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast1(5), 6);\n/// assert_eq!(atleast1(-3), -2);\n/// ```\nfn atleast1(x: i64) -> i64 {\n    cmp::max(x, 1)\n}\n```\n\nThis function uses the `cmp::max` function from the standard library to find the maximum of `x` and 1."}
{"task": "../bench/tasks-large/gen-atleast1-v2.json", "name": "atleast1", "raw": "```rust\nfn atleast1(x: i64) -> i64 {\n    if x < 0 {\n        panic!(\"Input must be non-negative.\");\n    }\n    x + 1\n}\n```\n\nThis function takes an integer `x` and returns `x + 1`, which satisfies the requirement of being non-negative."}
{"task": "../bench/tasks-large/gen-atleast10-v0.json", "name": "atleast10", "raw": "```rust\nuse std::cmp;\n\n/// Computes the maximum of `x` and 10.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast10(5), 15);\n/// assert_eq!(atleast10(-3), 7);\n/// ```\nfn atleast10(x: i64) -> i64 {\n    cmp::max(x, 10)\n}\n```\n\nThis function uses the `cmp::max` method to find the maximum of `x` and 10."}
{"task": "../bench/tasks-large/gen-atleast10-v1.json", "name": "atleast10", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.max` to `x` and the constant 10.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast10(5), 15);\n/// assert_eq!(atleast10(-3), 7);\n/// ```\nfn atleast10(x: i64) -> i64 {\n    cmp::max(x, 10)\n}\n```\n\nThis function uses the `cmp::max` method from the standard library to find the maximum of `x` and 10."}
{"task": "../bench/tasks-large/gen-atleast10-v2.json", "name": "atleast10", "raw": "```rust\nuse std::cmp;\n\nfn atleast10(x: i64) -> i64 {\n    cmp::max(10, x)\n}\n```\n\nThis function uses the `cmp::max` function to find the maximum of 10 and the input `x`, returning the result."}
{"task": "../bench/tasks-large/gen-atleast100-v0.json", "name": "atleast100", "raw": "```rust\nuse std::cmp;\n\n/// Computes the maximum of `x` and 100.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast100(50), 100);\n/// assert_eq!(atleast100(-10), 100);\n/// ```\nfn atleast100(x: i64) -> i64 {\n    cmp::max(x, 100)\n}\n```\n\nThis function uses the `cmp::max` method to find the maximum of `x` and 100."}
{"task": "../bench/tasks-large/gen-atleast100-v1.json", "name": "atleast100", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.max` to `x` and the constant 100.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast100(5), 105);\n/// assert_eq!(atleast100(-3), -3);\n/// ```\nfn atleast100(x: i64) -> i64 {\n    cmp::max(x, 100)\n}\n```\n\nThis function uses the `cmp::max` method from the standard library to find the maximum of `x` and 100."}
{"task": "../bench/tasks-large/gen-atleast100-v2.json", "name": "atleast100", "raw": "```rust\nuse std::cmp;\n\nfn atleast100(x: i64) -> i64 {\n    cmp::max(100, x)\n}\n```\n\nThis function uses the `cmp::max` function to ensure that the result is always at least 100."}
{"task": "../bench/tasks-large/gen-atleast2-v0.json", "name": "atleast2", "raw": "```rust\nuse std::cmp;\n\n/// Computes the maximum of `x` and 2.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast2(5), 7);\n/// assert_eq!(atleast2(-3), -1);\n/// ```\nfn atleast2(x: i64) -> i64 {\n    cmp::max(x, 2)\n}\n```\n\nThis function uses the `cmp::max` method to find the maximum of `x` and 2."}
{"task": "../bench/tasks-large/gen-atleast2-v1.json", "name": "atleast2", "raw": "```rust\nuse std::cmp;\n\nfn atleast2(x: i64) -> i64 {\n    cmp::max(x, 2)\n}\n```\n\nThis function uses the `cmp::max` function from the standard library to find the maximum of `x` and 2."}
{"task": "../bench/tasks-large/gen-atleast2-v2.json", "name": "atleast2", "raw": "```rust\nfn atleast2(x: i64) -> i64 {\n    if x < 0 {\n        return 0;\n    }\n    x + 1\n}\n```\n\nThis function takes an integer `x` and returns `x + 1`, which satisfies the condition `Nat.max(x, 2)`."}
{"task": "../bench/tasks-large/gen-atleast3-v0.json", "name": "atleast3", "raw": "```rust\nuse std::cmp;\n\n/// Computes the maximum of `x` and 3.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast3(5), 8);\n/// assert_eq!(atleast3(-1), 3);\n/// ```\nfn atleast3(x: i64) -> i64 {\n    cmp::max(x, 3)\n}\n```\n\nThis function uses the `cmp::max` method to find the maximum of `x` and 3."}
{"task": "../bench/tasks-large/gen-atleast3-v1.json", "name": "atleast3", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.max` to `x` and the constant 3.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast3(5), 8);\n/// assert_eq!(atleast3(-1), 2);\n/// ```\nfn atleast3(x: i64) -> i64 {\n    cmp::max(x, 3)\n}\n```\n\nThis function uses the `cmp::max` method from the standard library to find the maximum of `x` and 3."}
{"task": "../bench/tasks-large/gen-atleast3-v2.json", "name": "atleast3", "raw": "```rust\nfn atleast3(x: i64) -> i64 {\n    if x < 0 {\n        return 0;\n    }\n    if x == 0 {\n        return 0;\n    }\n    if x == 1 {\n        return 1;\n    }\n    if x == 2 {\n        return 2;\n    }\n    if x == 3 {\n        return 3;\n    }\n    if x == 4 {\n        return 4;\n    }\n    if x == 5 {\n        return 5;\n    }\n    if x == 6 {\n        return 6;\n    }\n    if x == 7 {\n        return 7;\n    }\n    if x == 8 {\n        return 8;\n    }\n    if x == 9 {\n        return 9;\n    }\n    if x == 10 {\n        return 10;\n    }\n    if x == 11 {\n        return 11"}
{"task": "../bench/tasks-large/gen-atleast4-v0.json", "name": "atleast4", "raw": "```rust\nuse std::cmp;\n\n/// Computes the maximum of `x` and 4.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast4(5), 9);\n/// assert_eq!(atleast4(-3), 3);\n/// ```\nfn atleast4(x: i64) -> i64 {\n    cmp::max(x, 4)\n}\n```\n\nThis function uses the `cmp::max` method to find the maximum of `x` and 4."}
{"task": "../bench/tasks-large/gen-atleast4-v1.json", "name": "atleast4", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.max` to `x` and the constant 4.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast4(5), 9);\n/// assert_eq!(atleast4(-3), 1);\n/// ```\nfn atleast4(x: i64) -> i64 {\n    cmp::max(x, 4)\n}\n```\n\nThis function uses the `cmp::max` method from the standard library to find the maximum of `x` and 4."}
{"task": "../bench/tasks-large/gen-atleast4-v2.json", "name": "atleast4", "raw": "```rust\nuse std::cmp;\n\nfn atleast4(x: i64) -> i64 {\n    cmp::max(4, x)\n}\n```\n\nThis function uses the `cmp::max` function to find the maximum of 4 and the input `x`, returning the result."}
{"task": "../bench/tasks-large/gen-atleast5-v0.json", "name": "atleast5", "raw": "```rust\nuse std::cmp;\n\n/// Computes the maximum of `x` and 5.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast5(3), 8);\n/// assert_eq!(atleast5(-1), 5);\n/// ```\nfn atleast5(x: i64) -> i64 {\n    cmp::max(x, 5)\n}\n```\n\nThis function uses the `cmp::max` method to find the maximum of `x` and 5."}
{"task": "../bench/tasks-large/gen-atleast5-v1.json", "name": "atleast5", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.max` to `x` and the constant 5.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast5(3), 8);\n/// assert_eq!(atleast5(-1), 4);\n/// ```\nfn atleast5(x: i64) -> i64 {\n    cmp::max(x, 5)\n}\n```\n\nThis function uses the `cmp::max` method from the standard library to find the maximum of `x` and 5."}
{"task": "../bench/tasks-large/gen-atleast5-v2.json", "name": "atleast5", "raw": "```rust\nuse std::cmp;\n\nfn atleast5(x: i64) -> i64 {\n    cmp::max(5, x)\n}\n```\n\nThis function uses the `cmp::max` function to find the maximum of 5 and the input `x`, returning the result."}
{"task": "../bench/tasks-large/gen-atleast7-v0.json", "name": "atleast7", "raw": "```rust\nuse std::cmp;\n\n/// Computes the maximum of `x` and 7.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atleast7(5), 7);\n/// assert_eq!(atleast7(-3), 7);\n/// ```\nfn atleast7(x: i64) -> i64 {\n    cmp::max(x, 7)\n}\n```\n\nThis function uses the `cmp::max` method to find the maximum of `x` and 7."}
{"task": "../bench/tasks-large/gen-atleast7-v1.json", "name": "atleast7", "raw": "```rust\nuse std::cmp;\n\nfn atleast7(x: i64) -> i64 {\n    cmp::max(x, 7)\n}\n```\n\nThis function uses the `cmp::max` function from the standard library to find the maximum of `x` and 7."}
{"task": "../bench/tasks-large/gen-atleast7-v2.json", "name": "atleast7", "raw": "```rust\nfn atleast7(x: i64) -> i64 {\n    if x >= 7 {\n        x\n    } else {\n        7 - x\n    }\n}\n```\n\nThis function checks if `x` is greater than or equal to 7 and returns it if true; otherwise, it returns `7 - x`."}
{"task": "../bench/tasks-large/gen-atmost1-v0.json", "name": "atmost1", "raw": "```rust\nuse std::cmp;\n\n/// Computes the minimum of `x` and 1.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost1(5), 5);\n/// assert_eq!(atmost1(-3), -3);\n/// ```\nfn atmost1(x: i64) -> i64 {\n    cmp::min(x, 1)\n}\n```\n\nThis function uses the `cmp::min` method to find the minimum of `x` and 1."}
{"task": "../bench/tasks-large/gen-atmost1-v1.json", "name": "atmost1", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.min` to `x` and the constant 1.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost1(5), 6);\n/// assert_eq!(atmost1(-3), -2);\n/// ```\nfn atmost1(x: i64) -> i64 {\n    cmp::min(x, 1)\n}\n```\n\nThis function uses the `cmp::min` method from the standard library to find the minimum of `x` and 1."}
{"task": "../bench/tasks-large/gen-atmost1-v2.json", "name": "atmost1", "raw": "```rust\nfn atmost1(x: i64) -> i64 {\n    if x < 0 {\n        return 0;\n    }\n    x + 1\n}\n```\n\nThis function takes an integer `x` and returns `x + 1`, which is the smallest possible value for `x` that satisfies the condition `atmost1(x) <= x`."}
{"task": "../bench/tasks-large/gen-atmost10-v0.json", "name": "atmost10", "raw": "```rust\nuse std::cmp;\n\n/// Computes the minimum of `x` and 10.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost10(5), 5);\n/// assert_eq!(atmost10(10), 10);\n/// ```\nfn atmost10(x: i64) -> i64 {\n    cmp::min(x, 10)\n}\n```\n\nThis function uses the `cmp::min` method to find the minimum of `x` and 10."}
{"task": "../bench/tasks-large/gen-atmost10-v1.json", "name": "atmost10", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.min` to `x` and the constant 10.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost10(5), 15);\n/// assert_eq!(atmost10(-3), -7);\n/// ```\nfn atmost10(x: i64) -> i64 {\n    cmp::min(x, 10)\n}\n```\n\nThis function uses the `cmp::min` method from the standard library to find the minimum of `x` and 10."}
{"task": "../bench/tasks-large/gen-atmost10-v2.json", "name": "atmost10", "raw": "```rust\nuse std::cmp;\n\nfn atmost10(x: i64) -> i64 {\n    cmp::max(10, x)\n}\n```\n\nThis function uses the `cmp::max` function to ensure that the result is always at most 10."}
{"task": "../bench/tasks-large/gen-atmost100-v0.json", "name": "atmost100", "raw": "```rust\nuse std::cmp;\n\n/// Computes the minimum of `x` and 100.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost100(50), 50);\n/// assert_eq!(atmost100(100), 100);\n/// ```\nfn atmost100(x: i64) -> i64 {\n    cmp::min(x, 100)\n}\n```\n\nThis function uses the `cmp::min` method to find the minimum of `x` and 100."}
{"task": "../bench/tasks-large/gen-atmost100-v1.json", "name": "atmost100", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.min` to `x` and the constant 100.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost100(5), 105);\n/// assert_eq!(atmost100(-3), -3);\n/// ```\nfn atmost100(x: i64) -> i64 {\n    cmp::min(x, 100)\n}\n```\n\nThis function uses the `cmp::min` method from the standard library to find the minimum of `x` and 100."}
{"task": "../bench/tasks-large/gen-atmost100-v2.json", "name": "atmost100", "raw": "```rust\nuse std::cmp;\n\nfn atmost100(x: i64) -> i64 {\n    cmp::min(100, x)\n}\n```\n\nThis function uses the `cmp::min` function to return the minimum of 100 and the input `x`."}
{"task": "../bench/tasks-large/gen-atmost2-v0.json", "name": "atmost2", "raw": "```rust\nuse std::cmp;\n\n/// Computes the minimum of `x` and 2.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost2(5), 3);\n/// assert_eq!(atmost2(-1), -1);\n/// ```\nfn atmost2(x: i64) -> i64 {\n    cmp::min(x, 2)\n}\n```\n\nThis function uses the `cmp::min` method to find the minimum of `x` and 2."}
{"task": "../bench/tasks-large/gen-atmost2-v1.json", "name": "atmost2", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.min` to `x` and the constant 2.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost2(5), 7);\n/// assert_eq!(atmost2(-3), -1);\n/// ```\nfn atmost2(x: i64) -> i64 {\n    cmp::min(x, 2)\n}\n```\n\nThis function uses the `cmp::min` method from the standard library to find the minimum of `x` and 2."}
{"task": "../bench/tasks-large/gen-atmost2-v2.json", "name": "atmost2", "raw": "```rust\nuse std::cmp;\n\nfn atmost2(x: i64) -> i64 {\n    cmp::max(0, x + 1)\n}\n```\n\nThis function uses the `cmp::max` function to ensure that the result is always non-negative, which is necessary for the problem's requirements."}
{"task": "../bench/tasks-large/gen-atmost3-v0.json", "name": "atmost3", "raw": "```rust\nuse std::cmp;\n\n/// Computes the minimum of `x` and 3.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost3(5), 3);\n/// assert_eq!(atmost3(-1), -1);\n/// ```\nfn atmost3(x: i64) -> i64 {\n    cmp::min(x, 3)\n}\n```\n\nThis function uses the `cmp::min` method to find the minimum of `x` and 3."}
{"task": "../bench/tasks-large/gen-atmost3-v1.json", "name": "atmost3", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.min` to `x` and the constant 3.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost3(5), 8);\n/// assert_eq!(atmost3(-3), -2);\n/// ```\nfn atmost3(x: i64) -> i64 {\n    cmp::min(x, 3)\n}\n```\n\nThis function uses the `cmp::min` method from the standard library to find the minimum of `x` and 3."}
{"task": "../bench/tasks-large/gen-atmost3-v2.json", "name": "atmost3", "raw": "```rust\nuse std::cmp;\n\nfn atmost3(x: i64) -> i64 {\n    cmp::min(x, 3)\n}\n```\n\nThis function uses the `cmp::min` function to find the minimum of `x` and 3, which is exactly what we need."}
{"task": "../bench/tasks-large/gen-atmost4-v0.json", "name": "atmost4", "raw": "```rust\nuse std::cmp;\n\n/// Computes the minimum of `x` and 4.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost4(5), 4);\n/// assert_eq!(atmost4(-3), -3);\n/// ```\nfn atmost4(x: i64) -> i64 {\n    cmp::min(x, 4)\n}\n```\n\nThis function uses the `cmp::min` method to find the minimum of `x` and 4."}
{"task": "../bench/tasks-large/gen-atmost4-v1.json", "name": "atmost4", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.min` to `x` and the constant 4.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost4(5), 9);\n/// assert_eq!(atmost4(-3), 1);\n/// ```\nfn atmost4(x: i64) -> i64 {\n    cmp::min(x, 4)\n}\n```\n\nThis function uses the `cmp::min` method from the standard library to find the minimum of `x` and 4."}
{"task": "../bench/tasks-large/gen-atmost4-v2.json", "name": "atmost4", "raw": "```rust\nuse std::cmp;\n\nfn atmost4(x: i64) -> i64 {\n    cmp::max(0, cmp::min(x, 4))\n}\n```\n\nThis function uses the `cmp::max` and `cmp::min` functions to ensure that the input `x` is within the range of 0 to 4, inclusive. The `atmost4` function then returns the maximum of these two values."}
{"task": "../bench/tasks-large/gen-atmost5-v0.json", "name": "atmost5", "raw": "```rust\nuse std::cmp;\n\n/// Computes the minimum of `x` and 5.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost5(3), 3);\n/// assert_eq!(atmost5(5), 5);\n/// ```\nfn atmost5(x: i64) -> i64 {\n    cmp::min(x, 5)\n}\n```\n\nThis function uses the `cmp::min` method to find the minimum of `x` and 5."}
{"task": "../bench/tasks-large/gen-atmost5-v1.json", "name": "atmost5", "raw": "```rust\nuse std::cmp;\n\nfn atmost5(x: i64) -> i64 {\n    cmp::min(x, 5)\n}\n```\n\nThis function uses the `cmp::min` function from the standard library to find the minimum of `x` and 5."}
{"task": "../bench/tasks-large/gen-atmost5-v2.json", "name": "atmost5", "raw": "```rust\nfn atmost5(x: i64) -> i64 {\n    if x < 0 {\n        return 0;\n    }\n    if x == 0 {\n        return 0;\n    }\n    if x == 1 {\n        return 1;\n    }\n    if x == 2 {\n        return 2;\n    }\n    if x == 3 {\n        return 3;\n    }\n    if x == 4 {\n        return 4;\n    }\n    if x == 5 {\n        return 5;\n    }\n    0\n}\n```\n\nThis function checks if the input `x` is less than 0, and returns 0 if true. If `x` is 0, 1, 2, 3, or 4, it returns that value. If `x` is 5, it returns 5. Otherwise, it returns 0."}
{"task": "../bench/tasks-large/gen-atmost7-v0.json", "name": "atmost7", "raw": "```rust\nuse std::cmp;\n\n/// Computes the minimum of `x` and 7.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost7(5), 5);\n/// assert_eq!(atmost7(3), 3);\n/// assert_eq!(atmost7(-1), -1);\n/// ```\nfn atmost7(x: i64) -> i64 {\n    cmp::min(x, 7)\n}\n```\n\nThis function uses the `cmp::min` method to find the minimum of `x` and 7."}
{"task": "../bench/tasks-large/gen-atmost7-v1.json", "name": "atmost7", "raw": "```rust\nuse std::cmp;\n\n/// Applies `Nat.min` to `x` and the constant 7.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(atmost7(5), 12);\n/// assert_eq!(atmost7(-3), -2);\n/// ```\nfn atmost7(x: i64) -> i64 {\n    cmp::min(x, 7)\n}\n```\n\nThis function uses the `cmp::min` method from the standard library to find the minimum of `x` and 7."}
{"task": "../bench/tasks-large/gen-atmost7-v2.json", "name": "atmost7", "raw": "```rust\nuse std::cmp;\n\nfn atmost7(x: i64) -> i64 {\n    if x < 0 {\n        return 0;\n    }\n    if x == 0 {\n        return 0;\n    }\n    if x == 1 {\n        return 1;\n    }\n    if x == 2 {\n        return 2;\n    }\n    if x == 3 {\n        return 3;\n    }\n    if x == 4 {\n        return 4;\n    }\n    if x == 5 {\n        return 5;\n    }\n    if x == 6 {\n        return 6;\n    }\n    if x == 7 {\n        return 7;\n    }\n    0\n}\n```\n\nThis function checks if the input `x` is less than 0, and returns 0 if true. If `x` is 0, 1, 2, 3, 4, 5"}
{"task": "../bench/tasks-large/gen-clamp-hilo.json", "name": "clamp", "raw": "```rust\nuse std::cmp;\n\n/// Clamps a given integer `x` into the range `[lo, hi]`.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(clamp(5, 1, 10), 5);\n/// assert_eq!(clamp(-3, -1, 1), -1);\n/// assert_eq!(clamp(7, 1, 10), 7);\n/// assert_eq!(clamp(-7, -1, 1), -1);\n/// ```\nfn clamp(x: i64, lo: i64, hi: i64) -> i64 {\n    cmp::max(lo, cmp::min(hi, x))\n}\n\nfn main() {\n    // Check function with provided data points\n    assert_eq!(clamp(5, 1, 10), 5);\n    assert_eq!(clamp(-3, -1, 1), -1);\n    assert_eq!(clamp("}
{"task": "../bench/tasks-large/gen-clamp-lohi.json", "name": "clamp", "raw": "```rust\nuse std::cmp;\n\n/// Clamps a given integer `x` into the range `[lo, hi]`.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(clamp(5, 1, 10), 5);\n/// assert_eq!(clamp(-3, -1, 1), -1);\n/// assert_eq!(clamp(7, 1, 10), 7);\n/// assert_eq!(clamp(-7, -1, 1), -1);\n/// ```\nfn clamp(x: i64, lo: i64, hi: i64) -> i64 {\n    cmp::max(lo, cmp::min(hi, x))\n}\n\nfn main() {\n    // Check function with provided data points\n    assert_eq!(clamp(5, 1, 10), 5);\n    assert_eq!(clamp(-3, -1, 1), -1);\n    assert_eq!(clamp("}
{"task": "../bench/tasks-large/gen-max3.json", "name": "max3", "raw": "```rust\nfn max3(a: i64, b: i64, c: i64) -> i64 {\n    match (a, b, c) {\n        (x, y, z) => x.max(y).max(z),\n        _ => panic!(\"All inputs must be non-negative integers.\"),\n    }\n}\n```\n\nThis function uses pattern matching to determine which of the three inputs is the largest and returns it. If all inputs are negative, it panics."}
{"task": "../bench/tasks-large/gen-mul1-v0.json", "name": "mul1", "raw": "```rust\nuse std::cmp;\n\n/// Computes `Nat.mul` of `x` and 1.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul1(5), 5);\n/// assert_eq!(mul1(-3), -9);\n/// ```\nfn mul1(x: i64) -> i64 {\n    match x {\n        0 => 0,\n        _ => x * 1,\n    }\n}\n\nfn main() {\n    // Test cases to verify the correctness of the function\n    assert_eq!(mul1(5), 5);\n    assert_eq!(mul1(-3), -9);\n    println!(\"All tests passed successfully.\");\n}\n```\n\nThis Rust function `mul1` takes an integer `x` as input and returns its product with 1. The function uses pattern matching to handle the case where `x` is 0, returning 0. For other values of `x`, it multiplies `x`"}
{"task": "../bench/tasks-large/gen-mul1-v1.json", "name": "mul1", "raw": "```rust\nuse std::cmp;\n\nfn mul1(x: i64) -> i64 {\n    match x {\n        0 => 0,\n        _ => x * 1,\n    }\n}\n```\n\nThis function takes an integer `x` as input and returns its product, which is `x * 1`."}
{"task": "../bench/tasks-large/gen-mul1-v2.json", "name": "mul1", "raw": "```rust\nfn mul1(x: i64) -> i64 {\n    x * 1\n}\n```\n\nThis function takes an integer `x` and returns its product, which is `x * 1`."}
{"task": "../bench/tasks-large/gen-mul10-v0.json", "name": "mul10", "raw": "```rust\nuse std::cmp;\n\n/// Computes `Nat.mul` of `x` and 10.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul10(5), 50);\n/// assert_eq!(mul10(-3), -30);\n/// ```\nfn mul10(x: i64) -> i64 {\n    x * 10\n}\n```\n\nThis function takes an `i64` as input and returns its product with 10. The `if` statement is used to handle cases where the input is negative, returning the original value if it is."}
{"task": "../bench/tasks-large/gen-mul10-v1.json", "name": "mul10", "raw": "```rust\nuse std::cmp;\n\n/// Multiplies a given integer `x` by 10.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul10(5), 50);\n/// assert_eq!(mul10(-3), -30);\n/// ```\nfn mul10(x: i64) -> i64 {\n    x * 10\n}\n```\n\nThis function takes an integer `x` as input and returns its value multiplied by 10. The function uses the `*` operator for multiplication and the `cmp` module to handle negative numbers correctly."}
{"task": "../bench/tasks-large/gen-mul10-v2.json", "name": "mul10", "raw": "```rust\nfn mul10(x: i64) -> i64 {\n    (x * 10) as i64\n}\n```\n\nThis function takes an integer `x` and returns its product with 10."}
{"task": "../bench/tasks-large/gen-mul100-v0.json", "name": "mul100", "raw": "```rust\nuse std::cmp;\n\n/// Computes `Nat.mul` of `x` and 100.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul100(5), 500);\n/// assert_eq!(mul100(-3), -300);\n/// ```\nfn mul100(x: i64) -> i64 {\n    x * 100\n}\n\nfn main() {\n    // Test cases to verify the correctness of the function\n    assert_eq!(mul100(5), 500);\n    assert_eq!(mul100(-3), -300);\n    println!(\"All tests passed successfully.\");\n}\n```\n\nThis Rust function `mul100` takes an integer `x` as input and returns its product with 100. The function uses basic arithmetic operations and comparison to ensure it works correctly for both positive and negative integers. The `main"}
{"task": "../bench/tasks-large/gen-mul100-v1.json", "name": "mul100", "raw": "```rust\nuse std::cmp;\n\n/// Multiply a number by 100.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul100(5), 500);\n/// assert_eq!(mul100(-3), -300);\n/// ```\nfn mul100(x: i64) -> i64 {\n    x * 100\n}\n```\n\nThis function takes an integer `x` as input and returns its value multiplied by 100. The function uses the `*` operator for multiplication and the `cmp` module to handle negative numbers correctly."}
{"task": "../bench/tasks-large/gen-mul100-v2.json", "name": "mul100", "raw": "```rust\nfn mul100(x: i64) -> i64 {\n    (x * 100) as i64\n}\n```\n\nThis function takes an integer `x` and returns its value multiplied by 100."}
{"task": "../bench/tasks-large/gen-mul2-v0.json", "name": "mul2", "raw": "```rust\nuse std::cmp;\n\n/// Computes `Nat.mul` of `x` and 2.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul2(3), 6);\n/// assert_eq!(mul2(-5), -10);\n/// ```\nfn mul2(x: i64) -> i64 {\n    x * 2\n}\n```\n\nThis function takes an `i64` as input and returns its double. The `if` statement is used to handle cases where the input is negative, returning the original value if it is."}
{"task": "../bench/tasks-large/gen-mul2-v1.json", "name": "mul2", "raw": "```rust\nuse std::cmp;\n\n/// Multiply two integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul2(3), 6);\n/// assert_eq!(mul2(-5), -10);\n/// ```\nfn mul2(x: i64) -> i64 {\n    x * 2\n}\n```\n\nThis function takes an integer `x` as input and returns its double."}
{"task": "../bench/tasks-large/gen-mul2-v2.json", "name": "mul2", "raw": "```rust\nfn mul2(x: i64) -> i64 {\n    (x * 2) as i64\n}\n```\n\nThis function takes an integer `x` and returns its double."}
{"task": "../bench/tasks-large/gen-mul3-v0.json", "name": "mul3", "raw": "```rust\nuse std::cmp;\n\n/// Computes `Nat.mul` of `x` and 3.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul3(5), 15);\n/// assert_eq!(mul3(-3), -9);\n/// ```\nfn mul3(x: i64) -> i64 {\n    x * 3\n}\n```\n\nThis function takes an `i64` as input and returns its product with 3. The function uses Rust's built-in arithmetic operations to perform the multiplication."}
{"task": "../bench/tasks-large/gen-mul3-v1.json", "name": "mul3", "raw": "```rust\nuse std::cmp;\n\nfn mul3(x: i64) -> i64 {\n    x * 3\n}\n```\n\nThis function takes an integer `x` as input and returns its value multiplied by 3."}
{"task": "../bench/tasks-large/gen-mul3-v2.json", "name": "mul3", "raw": "```rust\nfn mul3(x: i64) -> i64 {\n    (x * 3) as i64\n}\n```\n\nThis function takes an integer `x` and returns its value multiplied by 3."}
{"task": "../bench/tasks-large/gen-mul4-v0.json", "name": "mul4", "raw": "```rust\nuse std::cmp;\n\n/// Computes `Nat.mul` of `x` and 4.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul4(5), 20);\n/// assert_eq!(mul4(-3), -12);\n/// ```\nfn mul4(x: i64) -> i64 {\n    x * 4\n}\n```\n\nThis function takes an `i64` as input and returns its product with 4. The function uses Rust's built-in arithmetic operations to perform the multiplication."}
{"task": "../bench/tasks-large/gen-mul4-v1.json", "name": "mul4", "raw": "```rust\nuse std::cmp;\n\nfn mul4(x: i64) -> i64 {\n    x * 4\n}\n```\n\nThis function takes an integer `x` as input and returns its value multiplied by 4."}
{"task": "../bench/tasks-large/gen-mul4-v2.json", "name": "mul4", "raw": "```rust\nfn mul4(x: i64) -> i64 {\n    (x * 4) % 1_000_000_007\n}\n```\n\nThis function takes an integer `x` and returns its product of 4, modulo 1,000,000,007 to handle large numbers efficiently."}
{"task": "../bench/tasks-large/gen-mul5-v0.json", "name": "mul5", "raw": "```rust\nuse std::cmp;\n\n/// Computes `Nat.mul` of `x` and 5.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul5(3), 15);\n/// assert_eq!(mul5(-2), -10);\n/// ```\nfn mul5(x: i64) -> i64 {\n    x * 5\n}\n```\n\nThis function takes an `i64` as input and returns its product with 5. The `if` statement is used to handle cases where the input is negative, returning the original value if it is."}
{"task": "../bench/tasks-large/gen-mul5-v1.json", "name": "mul5", "raw": "```rust\nuse std::cmp;\n\nfn mul5(x: i64) -> i64 {\n    x * 5\n}\n```\n\nThis function takes an integer `x` as input and returns its value multiplied by 5."}
{"task": "../bench/tasks-large/gen-mul5-v2.json", "name": "mul5", "raw": "```rust\nuse std::cmp;\n\nfn mul5(x: i64) -> i64 {\n    (x * 5) % 1000000007\n}\n```\n\nThis function takes an integer `x` and returns `x * 5` modulo 1000000007, which is a common modulus operation used in cryptography and other cryptographic applications."}
{"task": "../bench/tasks-large/gen-mul7-v0.json", "name": "mul7", "raw": "```rust\nuse std::cmp;\n\n/// Computes `x * 7`.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(mul7(5), 35);\n/// assert_eq!(mul7(-3), -21);\n/// ```\nfn mul7(x: i64) -> i64 {\n    x * 7\n}\n```\n\nThis function takes an integer `x` as input and returns its product with 7. The function uses Rust's built-in arithmetic operations to perform the multiplication."}
{"task": "../bench/tasks-large/gen-mul7-v1.json", "name": "mul7", "raw": "```rust\nuse std::cmp;\n\nfn mul7(x: i64) -> i64 {\n    x * 7\n}\n```\n\nThis function takes an integer `x` as input and returns its product with 7."}
{"task": "../bench/tasks-large/gen-mul7-v2.json", "name": "mul7", "raw": "```rust\nuse std::cmp;\n\nfn mul7(x: i64) -> i64 {\n    (x * 7) % 1000000007\n}\n```\n\nThis function takes an integer `x` and returns `x * 7` modulo 1000000007, which is a common modulus operation used in cryptographic applications."}
{"task": "../bench/tasks-large/gen-nat_add.json", "name": "nat_add", "raw": "```rust\nuse std::cmp;\n\n/// Adds two non-negative integers using the provided arithmetic operations.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(nat_add(3, 5), 8);\n/// assert_eq!(nat_add(-1, -2), -3);\n/// assert_eq!(nat_add(0, 0), 0);\n/// ```\nfn nat_add(a: i64, b: i64) -> i64 {\n    cmp::max(a, b)\n}\n\n// Function to check the correctness of the `nat_add` function\nfn check() {\n    assert_eq!(nat_add(3, 5), 8);\n    assert_eq!(nat_add(-1, -2), -3);\n    assert_eq!(nat_add(0, 0), 0);\n    println!(\"All tests passed successfully.\");\n}\n\nfn main() {\n    // Call the check function to verify the correctness of the `nat_add` function\n    check"}
{"task": "../bench/tasks-large/gen-nat_max.json", "name": "nat_max", "raw": "```rust\nuse std::cmp;\n\n/// The maximum of two integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(nat_max(5, 3), 5);\n/// assert_eq!(nat_max(-1, -2), -1);\n/// ```\nfn nat_max(a: i64, b: i64) -> i64 {\n    cmp::max(a, b)\n}\n```\n\nThis function uses the `cmp::max` method from the standard library to find the maximum of the two input integers."}
{"task": "../bench/tasks-large/gen-nat_min.json", "name": "nat_min", "raw": "```rust\nuse std::cmp;\n\n/// Returns the minimum of two integers.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(nat_min(3, 5), 3);\n/// assert_eq!(nat_min(-1, -2), -2);\n/// ```\nfn nat_min(a: i64, b: i64) -> i64 {\n    cmp::min(a, b)\n}\n```\n\nThis function uses the standard library's `cmp::min` function to find the minimum of the two provided integers."}
{"task": "../bench/tasks-large/gen-nat_mul.json", "name": "nat_mul", "raw": "```rust\nuse std::cmp;\n\n/// Multiplies two non-negative integers using in-scope `Nat.mul`.\n///\n/// # Examples\n///\n/// ```\n/// assert_eq!(nat_mul(3, 4), 12);\n/// assert_eq!(nat_mul(-5, 3), -15);\n/// ```\nfn nat_mul(a: i64, b: i64) -> i64 {\n    cmp::max(a, b) * cmp::min(a, b)\n}\n\n// Check function to verify the correctness of the generated function.\nfn check() {\n    assert_eq!(nat_mul(3, 4), 12);\n    assert_eq!(nat_mul(-5, 3), -15);\n    assert_eq!(nat_mul(0, 5), 0);\n    assert_eq!(nat_mul(7, 7), 49);\n    println!(\"All tests passed successfully.\");\n}\n\nfn main() {\n    check();\n}\n```\n\n"}
{"task": "../bench/tasks-large/gen-sum3.json", "name": "sum3", "raw": "```rust\nfn sum3(a: i64, b: i64, c: i64) -> i64 {\n    match (a, b, c) {\n        (a, b, c) => a + b + c,\n        _ => panic!(\"All inputs must be non-negative integers.\"),\n    }\n}\n```\n\nThis function takes three non-negative integers as input and returns their sum. It uses pattern matching to handle all possible combinations of inputs, ensuring that the function is robust and handles invalid inputs gracefully."}
